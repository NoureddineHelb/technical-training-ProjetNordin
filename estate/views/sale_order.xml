<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="sale_order_view_inherit">
            <field name="name">sale.order.view.inherit1</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form" />
            <field name="arch" type="xml">
                <xpath expr="/form/sheet/notebook/page/field[@name='order_line']/tree/field[@name='tax_id']" position="after">
                    <field name="training_date" position="after"/>
                    <field name="employee_id" position="after"/>
                </xpath>
            </field>
        </record>

        <record id="view_order_form_inherited" model="ir.ui.view">
            <field name="name">Vue de formulaire de commande de vente</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="after">
                    <field name="manager_level" readonly="1"/>
                </field>
                <button name="action_confirm" position="after">
                    <button name="action_confirm" string="Confirmer la commande" type="object"/>
                </button>
            </field>
        </record>
    </data>
</odoo>
