<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="sale_order_view_inherit">
            <field name="name">sale.order.view.inherit1</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form" />
            <field name="arch" type="xml">
                <xpath expr="/form/sheet/notebook/page/field[@name='order_line']/tree/field[@name='tax_id']" position="after">
                    <field name="training_date" position="after" string="Date de formation"/>
                    <field name="employee_id" position="after" string="Employé affecté à la formation"/>
                </xpath>
            </field>
            <!-- Ajout des champs de date de formation et d'employé affecté à la formation dans le rapport de devis -->
            <t t-foreach="docs" t-as="o">
                <!-- ... -->
                <t t-foreach="o.order_line" t-as="l">
                    <!-- ... -->
                    <td><span t-field="l.training_date"/></td>
                    <td><span t-field="l.employee_id.name"/></td>
                    <!-- ... -->
                </t>
                <!-- ... -->
            </t>
        </record>
    </data>
</odoo>
